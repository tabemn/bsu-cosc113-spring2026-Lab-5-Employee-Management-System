# -bsu-cosc113-spring2026-Lab-5-Employee-Management-System
Design an employee management system using inheritance and polymorphism. Create a base class Employee with a protected instance variable name and a method calculateSalary() (which returns 0.0 in the base class). Create two concrete classes, FullTimeEmployee and PartTimeEmployee, that inherit from Employee.
